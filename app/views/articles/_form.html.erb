<%= simple_form_for(@article) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs my-3 form-control">
    <%= f.input :title %>
    <%= f.input :body %>
    <%= f.hidden_field :user_id, id: :article_user_id, value: current_user.id %>
    <%= f.association :category %>
    <br>
    <%= f.input :image %>
  </div>

  <div class="form-actions d-grid gap-2 btn btn-success updatebtn">
    <%= f.button :submit %>
  </div>
<% end %>
